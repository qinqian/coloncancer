## Manager usage
from multiprocessing import Process, Manager

manager = Manager()
d = manager.dict()

def f():
    d[1] += [4]
    print d

if __name__ == '__main__':
    d[1] = []
    p = Process(target=f)
    p.start()
    p.join()
