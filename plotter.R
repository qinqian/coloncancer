########################
## plot except heatmap
########################

library(geneplotter)
library(gclus)
library(hexbin)

myGeneplot <- function(){
  print;
  cPlot
  histStack
  ## library(rgl)
  ## scatter3d(sort(seq(length(t.p.value))), sort(t.p.value.unif), sort(t.p.value))
  ## smoothScatter(sort(t.p.value))
}

mycorplot <- function(){
}
